# Keel Agent Notes

`AGENTS.md` is the canonical repository instruction file for Keel. Claude agents
working in this repository should read and follow [`AGENTS.md`](./AGENTS.md)
rather than relying on a separate copy of the same rules.

Keep this file as a pointer unless Claude needs instructions that are genuinely
different from the shared repository rules.
